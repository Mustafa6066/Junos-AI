{
  "_meta": {
    "version": "13.0",
    "description": "Verified Junos CLI command dictionary for vMX 23.2R1.13. Used to validate AI-generated commands before presenting to user.",
    "last_updated": "2026-02-19"
  },
  "show_commands": {
    "ospf": [
      "show ospf neighbor",
      "show ospf neighbor detail",
      "show ospf neighbor instance {vrf}",
      "show ospf interface",
      "show ospf interface {interface} detail",
      "show ospf interface brief",
      "show ospf database",
      "show ospf database detail",
      "show ospf database router",
      "show ospf database network",
      "show ospf database summary",
      "show ospf database external",
      "show ospf database nssa",
      "show ospf route",
      "show ospf route detail",
      "show ospf overview",
      "show ospf statistics",
      "show ospf log"
    ],
    "bgp": [
      "show bgp summary",
      "show bgp neighbor",
      "show bgp neighbor {ip}",
      "show bgp neighbor {ip} detail",
      "show bgp group",
      "show bgp group summary",
      "show bgp group {name}",
      "show route advertising-protocol bgp {peer}",
      "show route receive-protocol bgp {peer}",
      "show route advertising-protocol bgp {peer} table {table}",
      "show route receive-protocol bgp {peer} table {table}",
      "show bgp neighbor {ip} received-prefix-count"
    ],
    "ldp": [
      "show ldp session",
      "show ldp session detail",
      "show ldp neighbor",
      "show ldp neighbor detail",
      "show ldp interface",
      "show ldp interface detail",
      "show ldp database",
      "show ldp database session {ip}",
      "show ldp route",
      "show ldp overview",
      "show ldp statistics",
      "show ldp traffic-statistics"
    ],
    "mpls": [
      "show mpls interface",
      "show mpls lsp",
      "show mpls lsp detail",
      "show mpls lsp name {name}",
      "show mpls lsp name {name} detail",
      "show mpls lsp extensive",
      "show mpls lsp ingress",
      "show mpls lsp transit",
      "show mpls lsp egress",
      "show rsvp interface",
      "show rsvp session",
      "show rsvp session detail",
      "show rsvp neighbor",
      "show rsvp neighbor detail",
      "show rsvp statistics",
      "show ted database",
      "show ted database extensive"
    ],
    "isis": [
      "show isis adjacency",
      "show isis adjacency detail",
      "show isis interface",
      "show isis interface {interface} detail",
      "show isis database",
      "show isis database detail",
      "show isis database extensive",
      "show isis route",
      "show isis overview",
      "show isis statistics",
      "show isis hostname",
      "show isis spf log",
      "show isis spf results"
    ],
    "interfaces": [
      "show interfaces terse",
      "show interfaces {interface} terse",
      "show interfaces {interface}",
      "show interfaces {interface} detail",
      "show interfaces {interface} extensive",
      "show interfaces {interface} statistics",
      "show interfaces descriptions",
      "show interfaces diagnostics optics",
      "show interfaces diagnostics optics {interface}",
      "show lldp neighbors",
      "show lldp neighbors detail",
      "show lldp neighbors interface {interface}"
    ],
    "routing": [
      "show route",
      "show route summary",
      "show route table {table}",
      "show route table inet.0",
      "show route table inet.3",
      "show route table mpls.0",
      "show route table {vrf}.inet.0",
      "show route {prefix}",
      "show route {prefix} detail",
      "show route {prefix} extensive",
      "show route {prefix} table {table}",
      "show route protocol {protocol}",
      "show route protocol ospf",
      "show route protocol bgp",
      "show route protocol isis",
      "show route protocol ldp",
      "show route protocol direct",
      "show route protocol static",
      "show route instance",
      "show route instance summary",
      "show route instance {name} detail",
      "show route forwarding-table",
      "show route forwarding-table destination {prefix}"
    ],
    "system": [
      "show chassis alarms",
      "show chassis environment",
      "show chassis hardware",
      "show chassis hardware detail",
      "show chassis routing-engine",
      "show chassis fpc",
      "show chassis fpc detail",
      "show system uptime",
      "show system alarms",
      "show system storage",
      "show system processes extensive",
      "show system processes extensive | match {pattern}",
      "show system commit",
      "show system license",
      "show system core-dumps",
      "show system memory",
      "show system statistics",
      "show log messages",
      "show log messages | last {n}",
      "show log messages | match {pattern}",
      "show ntp associations",
      "show ntp associations no-resolve",
      "show ntp status",
      "show snmp statistics"
    ],
    "vpn": [
      "show route instance summary",
      "show route instance {vrf}",
      "show route instance {vrf} detail",
      "show route table {vrf}.inet.0",
      "show bgp neighbor {ip} | match \"Type|State|Active|Received\"",
      "show services l2circuit connections",
      "show services l2circuit connections summary",
      "show evpn instance",
      "show evpn instance {name} detail",
      "show evpn database"
    ],
    "security": [
      "show firewall",
      "show firewall filter {name}",
      "show firewall filter {name} counter",
      "show firewall log",
      "show configuration firewall",
      "show configuration firewall filter {name}",
      "show policy-options",
      "show configuration policy-options"
    ],
    "bfd": [
      "show bfd session",
      "show bfd session detail",
      "show bfd session extensive"
    ],
    "configuration": [
      "show configuration",
      "show configuration | compare",
      "show configuration | compare rollback {n}",
      "show configuration protocols",
      "show configuration protocols ospf",
      "show configuration protocols bgp",
      "show configuration protocols ldp",
      "show configuration protocols mpls",
      "show configuration protocols isis",
      "show configuration protocols rsvp",
      "show configuration interfaces",
      "show configuration interfaces {interface}",
      "show configuration interfaces lo0",
      "show configuration routing-instances",
      "show configuration routing-instances {name}",
      "show configuration routing-options",
      "show configuration policy-options",
      "show configuration firewall",
      "show configuration system",
      "show configuration system services"
    ],
    "diagnostics": [
      "ping {destination}",
      "ping {destination} count {n}",
      "ping {destination} source {source}",
      "ping {destination} routing-instance {vrf}",
      "traceroute {destination}",
      "traceroute {destination} source {source}",
      "traceroute {destination} routing-instance {vrf}",
      "traceroute mpls ldp {prefix}",
      "traceroute mpls rsvp {lsp-name}"
    ]
  },
  "set_commands": {
    "ospf": [
      "set protocols ospf area {area} interface {interface}",
      "set protocols ospf area {area} interface {interface} interface-type p2p",
      "set protocols ospf area {area} interface {interface} passive",
      "set protocols ospf area {area} interface {interface} metric {metric}",
      "set protocols ospf area {area} interface {interface} hello-interval {seconds}",
      "set protocols ospf area {area} interface {interface} dead-interval {seconds}",
      "set protocols ospf area {area} interface {interface} authentication-key {key}",
      "set protocols ospf area {area} interface {interface} bfd-liveness-detection minimum-interval {ms}",
      "set protocols ospf traffic-engineering",
      "set protocols ospf reference-bandwidth {bw}"
    ],
    "bgp": [
      "set protocols bgp group {group} type internal",
      "set protocols bgp group {group} type external",
      "set protocols bgp group {group} local-address {ip}",
      "set protocols bgp group {group} peer-as {asn}",
      "set protocols bgp group {group} neighbor {ip}",
      "set protocols bgp group {group} family inet unicast",
      "set protocols bgp group {group} family inet-vpn unicast",
      "set protocols bgp group {group} family evpn signaling",
      "set protocols bgp group {group} cluster {id}",
      "set protocols bgp group {group} export {policy}",
      "set protocols bgp group {group} import {policy}",
      "set protocols bgp group {group} authentication-key {key}",
      "set protocols bgp group {group} authentication-algorithm {algo}",
      "set protocols bgp group {group} multihop ttl {ttl}",
      "set protocols bgp group {group} bfd-liveness-detection minimum-interval {ms}"
    ],
    "ldp": [
      "set protocols ldp interface {interface}",
      "set protocols ldp interface lo0.0",
      "set protocols ldp preference {preference}",
      "set protocols ldp session-protection",
      "set protocols ldp deaggregate",
      "set protocols ldp track-igp-metric",
      "set protocols ldp transport-address {ip}"
    ],
    "mpls": [
      "set protocols mpls interface {interface}",
      "set protocols mpls label-switched-path {name} to {destination}",
      "set protocols mpls label-switched-path {name} bandwidth {bw}",
      "set protocols mpls label-switched-path {name} primary {path}",
      "set protocols mpls traffic-engineering bgp-igp-both-ribs"
    ],
    "isis": [
      "set protocols isis interface {interface}",
      "set protocols isis interface {interface} point-to-point",
      "set protocols isis interface {interface} passive",
      "set protocols isis interface {interface} level {level} metric {metric}",
      "set protocols isis interface {interface} level {level} hello-authentication-key {key}",
      "set protocols isis interface {interface} level {level} hello-authentication-type md5",
      "set protocols isis level {level} authentication-key {key}",
      "set protocols isis level {level} authentication-type md5",
      "set protocols isis traffic-engineering family inet-mpls",
      "set protocols isis overload timeout {seconds}"
    ],
    "rsvp": [
      "set protocols rsvp interface {interface}",
      "set protocols rsvp interface {interface} bandwidth {bw}",
      "set protocols rsvp interface {interface} subscription {pct}%"
    ],
    "interfaces": [
      "set interfaces {interface} unit {unit} family inet address {ip/mask}",
      "set interfaces {interface} unit {unit} family mpls",
      "set interfaces {interface} unit {unit} family iso",
      "set interfaces {interface} description {description}",
      "set interfaces {interface} mtu {mtu}",
      "set interfaces {interface} disable",
      "set interfaces lo0 unit 0 family inet address {ip}/32"
    ],
    "routing_instances": [
      "set routing-instances {name} instance-type vrf",
      "set routing-instances {name} interface {interface}",
      "set routing-instances {name} route-distinguisher {rd}",
      "set routing-instances {name} vrf-target target:{asn}:{id}",
      "set routing-instances {name} vrf-import {policy}",
      "set routing-instances {name} vrf-export {policy}",
      "set routing-instances {name} protocols bgp group {group}"
    ],
    "policy": [
      "set policy-options policy-statement {name} term {term} from protocol {proto}",
      "set policy-options policy-statement {name} term {term} then accept",
      "set policy-options policy-statement {name} term {term} then reject",
      "set policy-options policy-statement {name} term {term} then next-hop self",
      "set policy-options community {name} members target:{asn}:{id}",
      "set policy-options prefix-list {name} {prefix}"
    ],
    "firewall": [
      "set firewall filter {name} term {term} from source-address {prefix}",
      "set firewall filter {name} term {term} from protocol {proto}",
      "set firewall filter {name} term {term} from destination-port {port}",
      "set firewall filter {name} term {term} then accept",
      "set firewall filter {name} term {term} then discard",
      "set firewall filter {name} term {term} then count {counter}",
      "set firewall filter {name} term {term} then log"
    ],
    "system": [
      "set system host-name {name}",
      "set system services ssh protocol-version v2",
      "set system services netconf ssh",
      "set system ntp server {ip}",
      "set system syslog host {ip} any any",
      "set system login idle-timeout {minutes}",
      "set system login message {banner}"
    ]
  },
  "delete_commands": [
    "delete protocols ospf area {area} interface {interface}",
    "delete protocols bgp group {group} neighbor {ip}",
    "delete protocols ldp interface {interface}",
    "delete protocols mpls interface {interface}",
    "delete protocols isis interface {interface}",
    "delete interfaces {interface}",
    "delete firewall filter {name} term {term}",
    "delete routing-instances {name}",
    "delete policy-options policy-statement {name}",
    "delete policy-options community {name}"
  ],
  "operational_commands": [
    "request system storage cleanup",
    "request system configuration rescue save",
    "request system software add {package}",
    "request system reboot",
    "clear ospf neighbor",
    "clear bgp neighbor {ip}",
    "clear ldp neighbor",
    "clear isis adjacency",
    "clear interfaces statistics {interface}",
    "clear firewall filter {name}",
    "rollback {n}",
    "commit check",
    "commit confirmed {minutes}",
    "commit comment {comment}"
  ],
  "common_patterns": {
    "interface_naming": "ge-{fpc}/{pic}/{port}.{unit}",
    "loopback": "lo0.0",
    "area_format": "0.0.0.{n} or 0 (for backbone)",
    "ip_format": "{octet1}.{octet2}.{octet3}.{octet4}/{mask}",
    "route_target": "target:{asn}:{id}",
    "route_distinguisher": "{ip}:{id} or {asn}:{id}"
  }
}
